# Cloudflare Pages — keep only safe caching hints. Do not override Content-Type on assets
# (can fight Cloudflare compression and trigger corrupted decoding in Firefox).

/index.html
  Cache-Control: public, max-age=0, must-revalidate
